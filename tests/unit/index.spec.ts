import { SERVICE_NAME } from '../../src/index';

describe('testappeter123-frontend', () => {
  it('should export SERVICE_NAME', () => {
    expect(SERVICE_NAME).toBe('testappeter123-frontend');
  });
});